<?xml version="1.0" encoding="utf-8"?>
<!-- updatePeriodMillis is 0 on purpose. The floor the system enforces is 30
     minutes, which is useless for link status and wakes the device for nothing;
     DockWidget pushes an update the moment ConnectionManager's state changes
     instead. -->
<appwidget-provider xmlns:android="http://schemas.android.com/apk/res/android"
    android:minWidth="180dp"
    android:minHeight="110dp"
    android:minResizeWidth="110dp"
    android:minResizeHeight="40dp"
    android:maxResizeWidth="320dp"
    android:maxResizeHeight="200dp"
    android:targetCellWidth="3"
    android:targetCellHeight="2"
    android:resizeMode="horizontal|vertical"
    android:widgetCategory="home_screen"
    android:updatePeriodMillis="0"
    android:initialLayout="@layout/widget_dock"
    android:previewLayout="@layout/widget_dock"
    android:previewImage="@drawable/ic_launcher"
    android:description="@string/widget_description" />
