# ---- macOS ----
.DS_Store
**/.DS_Store

# ---- Mac app: Electron / Node (droiddock 2) ----
**/node_modules/
droiddock 2/out/
droiddock 2/dist/
# keep droiddock 2/build/ — it holds the app icon (electron-builder resources)

# ---- Android: Gradle / Kotlin / Android Studio (droiddock-android) ----
**/.gradle/
**/.kotlin/
droiddock-android/build/
droiddock-android/app/build/
droiddock-android/**/build/
droiddock-android/captures/
droiddock-android/.cxx/
droiddock-tauri/future update
Cross-platform design specs
**/local.properties
*.iml
.idea/
# ---- logs / misc ----
*.log

# ---- Claude Code skill files ----
*_SKILL.md
SKILL.md
skills-lock.json
videos/

# ---- graphify knowledge graph (local only) ----
graphify-out/

# ---- droiddock-tauri: Rust build artifacts ----
droiddock-tauri/src-tauri/target/
droiddock 2
# ---- internal planning docs (local only, not for the public repo) ----
droiddock-tauri/AIRSYNC_GAP_ANALYSIS.md
droiddock-tauri/AIRSYNC_MOBILE_GAP.md
droiddock-tauri/CLAUDE.md
droiddock-tauri/PHASE16_PARITY_CHECKLIST.md
droiddock-tauri/checkpoint.md
droiddock-tauri/droiddock-tauri-prd-v3.md

# ---- throwaway Phase 0 spikes, and captured protocol corpus (may contain real personal data) ----
spikes/
droiddock-tauri/protocol-corpus/

# Scratch media for planned work — screen recordings blow past GitHub's 100 MB
# hard limit (a 177 MB .mp4 in here got a push rejected once). Note the
# trailing space in the real directory name.
future update /
*.mp4

# Local release-APK output. `assembleRelease` drops an 11 MB APK here; CI
# builds and uploads its own, so this is never worth committing.
droiddock-android/artifacts/
